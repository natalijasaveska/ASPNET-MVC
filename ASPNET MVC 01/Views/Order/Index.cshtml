@using ASPNET_MVC_01__Controllers.Models.ViewModels
@model OrderListViewModel;
@{
    @foreach(OrderViewModel order in @Model.Orders) {
	@Html.Partial("../Partials/_OrderInfo.cshtml", order);
    }
}

